# Nodeseek 客户端配置文件
# 放在 exe 同级目录下，程序启动时自动读取

# 域名默认值在仓库 src-tauri/src/lib.rs 的 get_default_url() 中修改
proxy_mode = system
disable_gpu = true
max_js_heap = 128

# 自定义配置示例：
# proxy_mode = custom
# custom_proxy = 127.0.0.1:10809
# disable_gpu = false